function drawHighest(hPoints)
     
     hPoints
     [m, n] = size(hPoints);
     hold on
     for h = 1 : m
         h
        line([hPoints(h, 1)-50, hPoints(h, 1)+50],[hPoints(h, 2), m(h, 2)],'Color',[1 0 0],'LineWidth',1);
     end
     hold off;
     